# Copy this file to .env.local for local development
# For production builds (Vercel / S3+CloudFront / Nginx), set these values
# in the platform's environment variable UI — do NOT commit .env.local

# Full URL of the deployed backend (empty = Vite proxy used in local dev)
VITE_API_URL=
